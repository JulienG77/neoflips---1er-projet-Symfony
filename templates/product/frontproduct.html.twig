{% extends 'base2.html.twig' %}
{% block title %} Fiche Produit: {% endblock %}

{% block body %}
<main role="main">
    <div class="container">
        <div class="row" style="
                                        align-items: center;
                                        justify-content: center">
            <div class="col-6">
                <img src="{{ asset('assets/img/') }}{{ produit.image }}" class="img-fluid mb-4 mt-4">
            </div>
            <div class="col-6" style="text-align: center">
                <h2> {{ produit.nom }}</h2>
                <p>{{ produit.reference }}</p>
                <p>{{ produit.description }}</p>
                <h5> Prix : {{ produit.prix }} €</h5>
                <a style="display: flex;flex-direction: column;justify-content: center;justify-items: center;margin-left: auto;margin-right: auto" href="{{ path('cartadd',{id : produit.id}) }}" class="btn btn-primary">Ajouter au panier</a>
        </div>
    </div>
</div>
</main>
{% endblock %}